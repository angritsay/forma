/**
 * ВРЕМЕННО: подмена всех платёжных ссылок на один тестовый товар.
 *
 * Заведена, чтобы прогнать оплату насквозь на маленькой сумме — от кнопки в
 * приложении до открывшегося доступа, — не платя за курс 2 990 ₽ и за год 7 990 ₽.
 *
 * **Как снять.** Поставить `null` в строке ниже и задеплоить. Одна строка, и
 * подмена исчезает везде: и в приложении, и на сайте. Ради этого она и сделана
 * одним переключателем, а не правкой ссылок по файлам — иначе половину забудешь,
 * и кто-нибудь заплатит 50 ₽ за годовую подписку.
 *
 * **Что подменяется:** кнопка «Открыть весь курс» в приложении, «Вступить» в
 * клубе, и форма заказа на сайте — и курсы, и подписка. **Что не
 * подменяется:** занятие с тренером (`content/site/booking.ts`) — это отдельный
 * поток, его никто не просил трогать.
 *
 * **Цена на экранах остаётся настоящей.** Приложение по-прежнему пишет «2 990 ₽»,
 * а форма попросит 50 ₽: сумма живёт на стороне Prodamus, и это правильно —
 * цена в ссылке была бы ценой, которую плательщик может поправить перед оплатой
 * (docs/SETUP.md §7.1). На время теста это расхождение и есть вся суть; в бою
 * его быть не должно, потому что подмены быть не должно.
 *
 * **И про то, чем это опознаётся на той стороне.** Вебхук отличает подписку от
 * курса **по сумме**: 1 990 — месяц, 7 990 — год, всё остальное — курс. Значит с
 * одним тестовым товаром два потока проверяются только по очереди:
 *
 *   1. сначала курс — 50 ₽ не равны цене плана, платёж уходит в активацию курса;
 *   2. потом клуб — и только тогда в Supabase задаётся `PLAN_MONTHLY_RUB=50`,
 *      после чего те же 50 ₽ опознаются как месячная подписка.
 *
 * Пока `PLAN_MONTHLY_RUB=50` задан, оплата курса на 50 ₽ откроет подписку, а не
 * курс. Поэтому порядок именно такой, и поэтому секрет убирается сразу после.
 */
export const TEST_PAYMENT_URL: string | null = null;
