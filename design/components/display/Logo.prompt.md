Леттеринг FORMA: толстое → тонкое (FOR 800, MA 200), первая F расширена ×1.22 — опорная буква. Латиница, читается на обоих языках.

```jsx
<Logo size={15} />               {/* шапка приложения */}
<Logo size={24} sergey />        {/* lockup с тренером */}
```

В HTML без React — спаны с теми же весами. Цвет наследуется (currentColor): на тёмном белый, на бумаге чёрный. Никаких точек, курсоров и знаков рядом.
