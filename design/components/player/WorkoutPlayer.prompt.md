Плеер тренировки — три фазы с цветовым кодом: объяснили (барвинок), делаем (мята), отдыхаем (персик).

```jsx
<WorkoutPlayer phase="do" exercise="Присед с собственным весом" timer="0:45" progress={62} playing onToggle={fn} />
```
