---
/**
 * Renders a bilingual legal document (legal.ts) with a table of contents.
 *
 * The contents are a numbered hairline list, not a boxed panel, and the sections carry the same
 * numerals so a reader can find their way back.
 */
import type { Locale } from '@/content/schema';
import { formatDate, l, t } from '@/i18n/index';
import type { LegalDocument } from './legal';

interface Props {
  locale: Locale;
  title: string;
  intro?: string;
  doc: LegalDocument;
  updatedAt: string;
}
const { locale, title, intro, doc, updatedAt } = Astro.props;
const num = (i: number) => String(i + 1).padStart(2, '0');
---

<article class="mx-auto max-w-3xl px-5 py-12 md:py-20">
  <p class="eyebrow-sentence">
    {t(locale, 'landing.legalUpdated', { date: formatDate(locale, updatedAt, 'long') })}
  </p>
  <h1 class="display mt-4 text-3xl text-balance md:text-5xl">{title}</h1>
  {intro && <p class="mt-5 text-lg leading-relaxed text-muted">{intro}</p>}

  <nav aria-label={t(locale, 'landing.legalContents')} class="mt-10 border-t border-border pt-5">
    <p class="eyebrow">{t(locale, 'landing.legalContents')}</p>
    <ol class="mt-3 grid gap-x-8 text-sm sm:grid-cols-2">
      {
        doc.sections.map((s, i) => (
          <li class="flex items-baseline gap-3 border-b border-border py-2">
            <span class="numeral text-xs text-muted-2" aria-hidden="true">
              {num(i)}
            </span>
            <a href={`#${s.id}`} class="text-muted hover:text-text">
              {l(s.heading, locale)}
            </a>
          </li>
        ))
      }
    </ol>
  </nav>

  <div class="mt-12 flex flex-col gap-12">
    {
      doc.sections.map((s, i) => (
        <section id={s.id} class="scroll-mt-24 border-t border-border pt-6">
          <h2 class="font-display flex items-baseline gap-4 text-lg">
            <span class="numeral text-sm text-muted" aria-hidden="true">
              {num(i)}
            </span>
            {l(s.heading, locale)}
          </h2>
          {s.paragraphs?.map((p) => (
            <p class="mt-4 leading-relaxed text-muted">{l(p, locale)}</p>
          ))}
          {s.bullets && (
            <ul class="mt-4 list-disc space-y-2 pl-5 leading-relaxed text-muted marker:text-muted-2">
              {s.bullets.map((b) => (
                <li>{l(b, locale)}</li>
              ))}
            </ul>
          )}
          {s.after?.map((p) => (
            <p class="mt-4 leading-relaxed text-muted">{l(p, locale)}</p>
          ))}
        </section>
      ))
    }
  </div>
</article>
